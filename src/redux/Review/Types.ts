type ReviewState = {
  submitting: boolean;
};

const SUBMIT_REVIEW = 'SUBMIT-REVIEW';
const SET_REVIEW_SUBMITTING = 'SET-REVIEW-SUBMITTING';

export default ReviewState;
export { SUBMIT_REVIEW, SET_REVIEW_SUBMITTING };
